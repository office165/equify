#!/bin/sh
# Blocks commits when valuation engine regressions are detected.
set -e

echo "▶ Running valuation engine regression suite (test:engine)..."
npm run test:engine --silent

echo "▶ Checking bridge render paths for manual currency sign concatenation..."
sh scripts/guard-no-manual-currency-sign.sh
